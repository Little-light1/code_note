export interface SubSystem {
  code: string;
  createBy: string;
  createTime: string;
  deleted: 0;
  id: string;
  name: string;
  remark: string;
  secret: string;
  state: {
    value: number;
    enum: string;
    desc: string;
  };
  updateBy: string;
  updateTime: string;
  version: string;
}